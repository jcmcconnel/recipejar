package recipejar

/**
 * iOS framework scaffolding marker (common domain is already in commonMain).
 * Full iOS app UI / signing is Phase 1A+; this target proves the KMP graph.
 */
object IosSharedMarker {
    const val TARGET: String = "ios"
}
